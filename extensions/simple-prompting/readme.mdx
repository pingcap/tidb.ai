import defaultTemplate from './template.liquid';

1. embedding user's message into vectors
2. similarity search `top_k` chunks using the vector
3. transform to prompt using `template`

The default template:

<pre><code className='language-liquid'>{defaultTemplate}</code></pre>

The template uses [Liquid](https://tryliquid.dev) syntax.

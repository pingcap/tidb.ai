{
  "name": "tidb-ai-parent",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.4.0",
  "scripts": {
    "build:widget-react": "pnpm run --filter 'packages/widget-react' build",
    "build:docker": "pnpm run --filter 'app' build:standalone"
  }
}

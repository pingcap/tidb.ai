import { ApiDoc } from "../../../../../../components/api-doc";

<ApiDoc
  method='GET'
  url='/api/v1/documents'
  description='List the loaded documents'
  request={[{
    description: <><code>page</code>: Page, <code>page_size</code>: Page Size</>
  }]}
  response={[{
    status: 200,
    description: 'Return the all documents.'
  }]}
/>

---

<ApiDoc
  method='PUT'
  url='/api/v1/documents'
  description='Upload documents'
  request={[{
    contentType: 'text/uri-list',
    description: <><strong>Recommended</strong> Upload a URL list to fetch.</>
  }, {
    contentType: 'multipart/form-data',
    description: 'Directly upload a file'
  }]}
  response={[{
    status: 200,
    description: 'No body returned'
  }]}
/>
